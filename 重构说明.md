# 尘安智眼 v4 重构说明（诊断 · 黄金 Prompt · 代码升级 · 比赛加分）

> 对照旧版 `app.py`（v3 单文件，279 行）的全面打磨记录。黄金 Prompt 全文见 `core/prompts.py`。

## 一、诊断报告：旧版的核心痛点

### 1. Prompt 层面（最致命）
| # | 痛点 | 后果 |
|---|---|---|
| P1 | **单一扁平 Prompt 包打天下**：视觉识别与数据分析共用一段 system prompt，角色只有一句“智能助手” | 视觉模型与文本模型的能力差异被抹平；没有专家人设，输出像“报数员” |
| P2 | **输出是自由 Markdown 表格**，无 JSON Schema、无字段约束 | 结果无法被程序解析，检测框、图表、台账、统计都无法驱动；漏列/串列/换个说法即崩溃 |
| P3 | **只有颜色分级，没有危险值、置信度、定位坐标、事故链、责任人、时限** | 专业度停留在“查到问题”，达不到“执法级工单”，评委看不到深度 |
| P4 | **传感器只做单点大小比较**（Python 里 `tsp<=limit`），无时间序列、无斜率、无连续超标、无趋势 | 无法回答“正在恶化还是偶发”；用户明确要求的“夏季高温对混凝土浇筑影响”等工艺机理完全缺失 |
| P5 | **反幻觉只有一句“不要编造”**，没有置信度纪律、看不清的处置规则、引用白名单 | 模型容易脑补标准条款号、把休息区人员判违章，比赛现场被追问会露怯 |
| P6 | 检查项偏环保（扬尘/噪声），**缺安全帽之外的施工安全大类**（明火动火、脚手架、临边洞口、临时用电、吊装） | 与“工地安全监测”题目和“为工友谋幸福”主题贴合度不够 |

### 2. 代码与工程层面
| # | 痛点 | v4 对策 |
|---|---|---|
| C1 | 279 行单文件，配置/Prompt/调用/UI 全耦合，限值在 Python 与 Prompt 里各写一遍（口径漂移） | 拆为 `config/prompts/knowledge/analytics/charts/vision_overlay/llm/demo/reporting` 9 个模块，限值单一事实来源 |
| C2 | OpenAI 客户端**未设超时**，仅一个笼统 `except`；鉴权失败、限流、模型名错误、JSON 异常混为一谈 | 超时+指数退避重试；异常翻译为中文友好提示；JSON 去围栏→括号截取→模型自修复杂用 |
| C3 | 图片不校验格式/大小/可解码；PNG 被硬编码成 `image/jpeg`；读过一次的文件指针有复用风险 | PIL 校验、10MB 限制、MIME 正确映射、统一字节流 |
| C4 | 天气写入 `number_input` 的 default，**控件已创建后不会随 session_state 刷新**（Streamlit 经典坑） | 改用 widget key + `st.rerun()` 的标准回填模式 |
| C5 | **零数据可视化**，单点数字配手写 HTML 卡片 | Plotly 三联时序图（阈值虚线+超标红叉+温湿双轴）、风险仪表盘；无 plotly 自动降级 `st.line_chart` |
| C6 | 无时序入口（只填一个数） | CSV 上传 / `st.data_editor` 在线录入 / 单点速测 三种输入，附模板下载 |
| C7 | “知识库在 GitHub”只是背景，实际只读本地且**全量 1.4 万字符硬塞 Prompt** | 规则精要压缩注入（约省 30% Token）+ 隐患分类码命中后召回全文；GitHub raw 一键同步、失败降级本地 |
| C8 | 无结果历史、无 JSON 出口、无离线兜底（路演断网即翻车） | 会话台账、JSON+Markdown 双下载、内置演示图/样例数据的**演示模式** |

## 二、黄金 Prompt 库（设计说明，全文见 `core/prompts.py`）

采用 **Role-Context-Task-Rules-Format** 结构（融合 CRISPE 的能力与步骤细化），两套提示词各自独立：

1. **图像识别系统提示词 `build_vision_system_prompt`**
   - 复合专家人设：注册安全工程师 + 环境监理 + 职业卫生医师；
   - 内置 **18 类隐患分类目录**（分类码、默认等级、引用标准、知识库文件、识别要点），模型只能从目录取码；
   - 每个隐患走“定位→定性→定级→举证→处置”五步推理，输出 `danger_level 1~5`（附危险值矩阵）、`confidence`、归一化 `bbox`、`accident_chain`、`standard_refs`、`rectification(动作/时限/责任人/语音话术)`；
   - 置信度纪律：<0.5 不得定红线、看不清进 `uncertainties`、合格项不输出、无知识库全文的安全类只许引标准名称不许编条款。
2. **数据检测系统提示词 `build_sensor_system_prompt`**
   - 人设增加**施工工艺工程师**；程序先把质控、统计、斜率、WBGT、呼吸带折算全部算准（features JSON），Prompt 明确“**数值结论以 features 为准，模型不得重算**”；
   - 内置**专业机理库**：高温→混凝土坍落度损失/冷缝/塑性收缩裂缝与养护措施、高温高湿→WBGT 与热射病、粉尘→矽肺剂量、dB 对数尺度（+3dB 时间减半）、趋势语义与外推纪律；
   - 强制输出多因子耦合研判、**混凝土浇筑/室外作业工艺窗口**、分级预警、预计达阈时间、完整 Markdown 报告。
3. 两套 Prompt 均：**只输出一个 JSON 对象、禁止代码围栏**，并各附一个 few-shot 样例锚定格式。

## 三、代码亮点（中文注释已标在源码中）

- `core/analytics.py`：最小二乘斜率、最长连续超标段、WBGT 双线性插值、场界→呼吸带系数折算、DB61 背景修正三档、传感器掉点/卡死质控；趋势分类“方向性优先”，避免把“上升后小幅回落”误判为波动。
- `core/vision_overlay.py`：纯 PIL 检测框叠加，半透明填充、标签防碰撞自动选位、顶部中文统计条（不依赖 OpenCV，断网可用）。
- `core/llm.py`：视觉/文本模型双通道、3 次退避重试、JSON 三级修复、演示模式旁路。
- `core/knowledge.py`：规则精要抽取（只留限值表与 IF 规则）、分类码→法条全文召回、GitHub raw 双源、关键词检索。
- `app.py`：五 Tab（总览/视觉/数据/知识库/台账）+ `st.status` 流水线状态 + 全链路 try/except 友好兜底。

## 四、比赛加分项建议（站在评委视角，按性价比排序）

### ⭐ 加分点 1：检测框“AI 巡检成片” + 语音闭环（半天工作量，现场最炸）
当前已实现**原图叠加红/橙/黄检测框、危险值标签与顶部风险条**。再进一步：
- 用 `st.camera_input` 接入摄像头连拍，或循环读取一个文件夹的图片，用 `st.image` 做 3 秒/帧的“巡检轮播”，配 `st.autorefresh`（streamlit-autorefresh）模拟实时大屏；
- 把 JSON 里的 `voice_prompt` 接浏览器 `speechSynthesis`（components.html 约 20 行 JS）做**现场语音喊话**，完整复刻知识库 `06-安全帽口罩-规则.md` 的“语音提示→5 秒回检→升级工单”三级闭环。
- 评委记忆点：**“发现隐患—框出来—喊出来—派工单”一条链全在屏幕上发生**，从“识别 Demo”升级为“处置闭环产品”。

### ⭐ 加分点 2：传感器“施工工艺窗口”红绿灯（已有底座，1~2 小时打磨）
当前 AI 已输出 `process_window`（混凝土浇筑适宜性 + 室外作业限时）。建议在数据 Tab 顶部做两张**大号红绿灯卡片**（适宜/受限/禁止 + 原因 + 措施），并在 Plotly 上叠加“未来 30 分钟趋势虚线 + 预计达阈时刻”。
- 评委记忆点：别的队停在“超标报警”，你们回答的是**“今天下午能不能浇筑、几点必须停工、为什么”**——直接呼应“为人民建好房（质量）+ 为工友谋幸福（健康）”双主题。

### 备选（有余力再做）
- 报告一键生成带二维码的**整改工单图片**（PIL 即可），扫码查看原图与检测框，扫码动作天然适合路演；
- 台账接 SQLite 持久化 + 按项目/班组统计隐患画像（帕累托图），讲“数据驱动管理”；
- 知识库升级为向量 RAG（chromadb + embedding），在答辩 PPT 里作为“技术深度”备选问答，但当前关键词+分类码映射在 11 份文件规模下更稳、可解释，不建议赛前贸然换。

## 五、验证记录

- `python -m py_compile`：全部 11 个 py 文件通过；
- `python tests/smoke_test.py`：知识库加载/精要压缩、分类映射、Prompt 构建、特征工程（示例数据正确识别 TSP/噪声双超标、WBGT≈33.2℃、呼吸带 3.64 mg/m³）、演示结果、检测框出图、Plotly 图表、Markdown 报告全部通过；
- `python tests/apptest_smoke.py`：Streamlit AppTest 无头执行，演示模式下视觉巡检与数据研判两条点击链路无异常、报告写入台账。
